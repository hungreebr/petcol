<%= simple_form_for(@post) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
<%= f.input :pet_id, collection: @petlist %>
<%= f.input :picture %>
    <%= f.input :story %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
